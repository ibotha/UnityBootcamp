using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpringInit : MonoBehaviour
{
	// public SpringJoint		springJoint;
	public Transform	rhing;
	// public GameObject		link;
    // Start is called before the first frame update
    void Start()
    {
		// GetComponent<Rigidbody>().centerOfMass = rhing.position;//rhing.position;
		// GetComponentInChildren<SpringJoint>().connectedBody = link.GetComponent<Rigidbody>();/
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
